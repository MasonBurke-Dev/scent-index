# Project Rules

- **Scope Restriction:** You are ONLY permitted to edit HTML (`.html`) and CSS (`.css`) files.
- **Prohibition:** Do NOT modify, create, or delete any Java files (`.java`) or other backend configuration files (e.g., `pom.xml`, `application.properties`) unless explicitly instructed otherwise in a future turn.
